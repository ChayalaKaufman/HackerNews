@using HackerNewsLibrary
@model IEnumerable<News>

@foreach (News story in Model)
{
    <div class="row">
        <div class="col-md-4 col-md-offset-3 well">
            <h3>@story.Title</h3>
            <a href="@story.Url">View Full Story</a>
        </div>
    </div>
}